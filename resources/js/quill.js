export default function quill({
    state,
    statePath,
    disabled = false,
    locale = 'en',
    placeholder = null,
}) {
    return {
        state,
        statePath,
        disabled,
        locale,
        placeholder,

        init() {

        },

        initEditor() {

        },
    };
}
